# 经验落盘：用户偏好规则

**落盘日期:** 2026-07-29
**类型:** 用户偏好/工作规则

---

## 规则清单

### 1. 推荐选项位置
- **规则:** 每次提供选项时，把AI推荐的选项放在 **A 位**
- **原因:** 用户习惯性选择第一个选项，推荐项放A位减少决策成本
- **执行:** 在所有选项列表中，推荐项始终为A

### 2. 命令格式统一
- **规则:** 所有终端命令统一使用 **PowerShell** 格式
- **原因:** Windows环境默认使用PowerShell，避免cmd和PowerShell混用导致用户困惑
- **执行:** 
  - 删除目录: `Remove-Item -Recurse -Force "路径"`
  - 删除文件: `Remove-Item -Force "路径"`
  - 创建目录: `New-Item -ItemType Directory -Path "路径" -Force`
  - 复制文件: `Copy-Item "源" "目标" -Force`
  - 进入目录: `Set-Location "路径"`
  - 运行命令: `& "命令" 参数` 或直接 `命令 参数`

### 3. 经验落盘规则
- **规则:** 每次完成重要修复或功能后，必须落盘经验
- **原因:** 避免重复踩坑，积累项目知识
- **执行:** 创建lessons_learned_*.md文件，更新index.md索引

### 4. 数据库访问规则
- **规则:** 使用useDatabase()上下文函数访问数据库，避免直接使用db对象
- **原因:** 直接使用db可能导致"Access to closed resource"错误
- **执行:** 使用getStocks()、getKlineByCode()等上下文提供的函数

### 5. 导航参数规则
- **规则:** 导航参数必须做空值保护，使用可选链和默认值
- **原因:** 避免"Cannot read property of undefined"错误
- **执行:** 使用`route?.params?.stock`或提供默认值

### 6. 详情页布局规则
- **规则:** 头部控制在两行内，移除无意义的分数显示
- **原因:** 用户反馈三行布局浪费空间，分数显示无实际意义
- **执行:** 第一行放代码+名称+信号徽章，第二行放价格+涨跌

### 7. 进度条显示规则
- **规则:** 进度条应显示实际业务数量（如股票数），而非内部权重
- **原因:** 用户反馈"10/10"不明晰，应像K线补齐那样显示"150/1745"
- **执行:** 分两阶段显示：导入阶段（权重）+ 加载阶段（实际股票数）

### 8. 数据来源标注规则
- **规则:** 使用实时数据时必须在UI上标注数据来源和日期
- **原因:** 用户需要知道数据是否最新，避免误解
- **执行:** 在走势概况中显示"实时数据（YYYY-MM-DD）"或"数据库数据"

---

## 执行检查清单

在输出选项列表前，自查：
- [ ] 推荐项是否在A位？
- [ ] 选项数量是否控制在2-4个？
- [ ] 是否附带推荐理由？
- [ ] 详情页头部是否控制在两行？
- [ ] 进度条是否显示实际业务数量？
- [ ] 数据来源是否标注清楚？

---

**落盘完成:** 2026-07-30 16:30
