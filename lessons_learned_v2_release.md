# 经验落盘：正式联网版 v2.0.0 发布

> 主题：CSV 功能移除 + 全量补齐分批优化 + 版本升级
> 落盘日期：2026-07-28

---

## 1. 本次变更要点

### 1.1 移除 CSV 导出功能
- **背景**：CSV 导出功能在实际使用中体验不佳（格式复杂、中文兼容差、用户更偏好 Markdown 报告）
- **删除范围**：
  - `AnalysisService.ts` 中的 `generateCSV` 函数
  - `StrategyScreen.tsx` 中的 `handleExportCSV` 方法、导出按钮、相关样式
  - `HelpScreen.tsx` 中 CSV 相关说明文字
- **保留**：Markdown 报告导出功能完整保留（Top20 / 全量）
- **教训**：功能不是越多越好，移除低频/体验差的功能可以降低维护成本和用户认知负担

### 1.2 全量补齐改为分批循环
- **问题**：原实现硬限制 50 只，超出部分不补齐，用户数据不完整
- **方案**：每批 50 只循环处理，跨批累计进度
- **进度展示**：`第X/Y批: code (累计/总数)`，让用户感知整体进度
- **代码模式**：外层 for 循环 + 内层 fillBatch，累加 success/failed/skipped

### 1.3 版本升级 v2.0.0
- app.json 版本号：1.0.0 → 2.0.0
- 使用说明书同步升级，新增「K线补齐功能」章节
- 章节编号顺延：原五章 → 八章

---

## 2. 文档完善实践

### 2.1 使用说明书新增章节结构
- 功能概述（是什么）
- 两种方式对比表（全量 vs 单股）
- 操作步骤（分步骤说明）
- 进度说明（文案格式解读）
- 机制说明（三源降级/增量/缓存/熔断/互斥锁）
- 注意事项（网络/停牌/失败）

### 2.2 版本号管理
- app.json 中的 version 字段是 Expo 识别应用版本的关键
- 文档版本号应与代码版本号同步
- changelog 中标注版本号便于追溯

---

## 3. 质检 checklist（本阶段已验证）

- [x] `npx tsc --noEmit` 无错误
- [x] `npx jest --ci --silent` 73/73 通过
- [x] 删除 CSV 功能未影响 Markdown 报告导出
- [x] 文档章节编号连贯
- [x] 版本号同步（app.json + 说明书 + changelog）
